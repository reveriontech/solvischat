# Cloudflare Pages configuration
name = "stella-chat"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# GEMINI_API_KEY must be set for Production (Dashboard → Environment variables
# or `wrangler pages secret put GEMINI_API_KEY --project-name=stella-chat`).
# Do not commit real keys here.
